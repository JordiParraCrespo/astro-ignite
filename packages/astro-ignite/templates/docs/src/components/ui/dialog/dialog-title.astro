---
import { cn } from '@/lib/cn';

export type Props = { id?: string; class?: string };
const { id, class: className } = Astro.props;
---

<h2 id={id} class={cn('m-0 text-lg leading-tight font-semibold text-fg', className)}><slot /></h2>
