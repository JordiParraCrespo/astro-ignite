---
import { cn } from '@/lib/cn';

export type Props = { id?: string; class?: string };
const { id, class: className } = Astro.props;
---

<h2 id={id} class={cn('text-lg font-semibold leading-tight text-fg m-0', className)}><slot /></h2>
