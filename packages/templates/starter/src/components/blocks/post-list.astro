---
import { cn } from '@/lib/cn';

export type Props = { class?: string };
const { class: className } = Astro.props;
---

<div class={cn('flex flex-col max-w-[720px] mx-auto', className)}>
  <slot />
</div>
