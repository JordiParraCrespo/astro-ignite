# Review — wire-local-lighthouse-against-a-preview (run 2026-05-19T03-52-23Z)

Verdict: **APPROVED**

Reviewer: headless autopilot reviewer agent (2026-05-19).

The change wires the local Lighthouse pipeline end-to-end: a real
`scripts/perf/run.mjs` that boots a preview server, runs Lighthouse,
parses the LHR, and tears the server down on every exit path; an
idempotent Chrome-for-Testing installer; an `npm-cache-writable`
doctor check; the source-of-truth `aig-runner.service` systemd unit;
and the dual-gate documentation in root `AGENTS.md`. The pure
comparator helper is unit-tested. The graceful-skip path (no Chrome
on PATH → exit 0) is exercised by every audit + perf-budget run on
this host.

## T1 — Tests

- `pnpm typecheck` (filtered to `--filter=!playground`): ✅ exit 0.
  - `apps/playground/` fails (`astro: not found`) because its
    `node_modules` is missing; it is regenerated by
    `scripts/scaffold-test.mjs` in CI and not relevant to this
    change (pre-existing — see `impl.md` open question 3).
- `pnpm test`: ✅ exit 0; 9 tests pass in `packages/astro-ignite`.
- `node --test scripts/perf/run.test.mjs`: ✅ 11 assertions pass
  (compareAgainstBudget happy-path, LCP-fail, score-fail,
  missing-metric "fail loudly"; compareTransfer pass/fail/missing;
  resolveTarget routing).

Scenario coverage (re-derived from `proposal.md` against the repo
state):

- S1 install idempotence → `scripts/doctor/install-chrome.mjs:54-70`
  (symlink fast-path) — file-level evidence; manual T25.
- S2 first-time install → `scripts/doctor/install-chrome.mjs:72-108`;
  `--dry-run` was exercised (T24) and resolved to
  `chrome-for-testing-public/131.0.6778.85/linux64/chrome-linux64.zip`.
- S3 `--prefix`/`--bindir` overrides →
  `scripts/doctor/install-chrome.mjs:39-50`.
- S4 doctor recognises install →
  `scripts/doctor/chrome-installed.mjs:22-33` probes
  `/usr/local/bin/chrome` and `$HOME/.local/bin/chrome`.
- S5 Lighthouse end-to-end → `runLighthouseAgainst` at
  `scripts/perf/run.mjs:233-291` + `main()` runOnce; operator T27.
- S6 metric busts → `scripts/perf/run.test.mjs:67-79` ✅ (automated).
- S7 score below floor → `scripts/perf/run.test.mjs:81-92` ✅
  (automated).
- S8 within budget → `scripts/perf/run.test.mjs:57-65` ✅
  (automated).
- S9 cleanup on normal exit → `scripts/perf/run.mjs:332-345`
  cleanup in `finally`.
- S10 SIGINT cleanup → `scripts/perf/run.mjs:346-353` SIGINT/SIGTERM
  handlers; operator T28.
- S11 `--critical-css` preserved → `scripts/perf/run.mjs:380-398`;
  audit invocation prints
  `✅ Critical CSS inlined in apps/docs/dist/index.html`.
- S12 graceful skip when chrome absent → `scripts/perf/run.mjs:181-196`
  + the branch at `:421-429`. Confirmed live: every run of the
  audit and perf-budget on this host prints
  `skipped — chrome not installed; run scripts/doctor/install-chrome.mjs`
  and exits 0.
- S13 systemd unit declares writable npm cache →
  `autopilot/systemd/aig-runner.service:29-32`:
  `ProtectSystem=strict`, `PrivateTmp=true`,
  `ReadWritePaths=%h/.npm %h/.cache /opt/chrome-for-testing`. ✅
- S14 npm-cache-writable doctor check →
  `scripts/doctor/npm-cache-writable.mjs` is wired and exercised by
  `pnpm doctor`; on this host (systemd unit not yet deployed) it
  emits the expected `warn` with the fix-hint naming
  `autopilot/systemd/aig-runner.service`.
- S15 AGENTS.md dual-gate doc → `AGENTS.md:153-170`: `## Performance
gates`, `### Graceful skip on missing Chrome`, `### How to make the
local gate green`. ✅
- S16 per-page metrics in stdout → covered by the `record(…)` +
  `formatMetric` path at `scripts/perf/run.mjs:75-104`; operator
  T27.
- S17 boundary → `git diff --name-only main` matches the proposal
  S17 list exactly (see "Commits scoped to design.md" below).

## T2 — Invariant audits

`pnpm audit:invariants --change wire-local-lighthouse-against-a-preview`:
✅ exit 0. Re-derived audit map (overwritten this review run, then
restored to the implementer-committed version since the audit-report
generator does not run prettier on its own output):

- I1 Lighthouse home (`node scripts/perf/run.mjs --page /`) — PASS
  via graceful-skip (chrome absent on this host).
- I2 Lighthouse inner (`--page /blog`) — PASS via graceful-skip.
- I3 Total transfer (`--transfer`) — PASS via graceful-skip.
- I4 Critical CSS (`--critical-css`) — PASS (real assertion —
  `apps/docs/dist/index.html` contains inline `<style>`).
- I5 No undeclared runtime dep (`--deps`) — PASS (starter 12,
  docs 8).

The chrome-bound invariants (I1, I2, I3) are exercised via the
graceful-skip branch on this runner; the implementer flagged that
operator-driven T27 will exercise the real Lighthouse path post-
install. The change does **not** loosen the canonical budget — it
wires the pipeline so the budget can be measured for the first
time.

## T3 — Perf budget

Applicable: yes (capability includes `templates-perf`).

`pnpm perf:budget --change wire-local-lighthouse-against-a-preview`:
✅ exit 0. Findings:

- ✅ packages/templates/starter dep count — 12 runtime deps
- ✅ packages/templates/docs dep count — 8 runtime deps
- ✅ Lighthouse run — skipped — chrome not installed; run
  scripts/doctor/install-chrome.mjs

The local advisory gate is green via the graceful-skip path, which
is the documented behaviour pre-Chrome-install. CI's
`Lighthouse CI (mobile)` (the authoritative gate) is unchanged.

## Tasks

Phase 1–6 (T1–T24, T30, T31, T32) — all `[x]` and verified.

The five unchecked tasks all sit in Phase 7 Verification:

- T25 sudo `install-chrome.mjs` on the runner — `[ ]`. Justified
  in `impl.md` open question 2: no sudo in this implementer
  session. The dry-run (T24) confirmed the manifest resolution
  end-to-end. ⚠️ operator follow-up.
- T26 deploy systemd unit + re-run `pnpm doctor` — `[ ]`.
  Justified same as T25 (sudo + `systemctl`). ⚠️ operator
  follow-up.
- T27 `pnpm perf:budget --page /` end-to-end on real Chrome —
  `[ ]`. Justified — requires T25 first. ⚠️ operator follow-up.
- T28 SIGINT cleanup verification — `[ ]`. Justified — requires
  a real Chrome run to interrupt. ⚠️ operator follow-up.
- T29 graceful-skip — `[ ]` in `tasks.md` but `notes.md` already
  records this as ✅. Bookkeeping mismatch; not a blocker (the
  audit run during this review re-confirmed the skip-and-exit-0
  behaviour, so the underlying behaviour is verified). Implementer
  should flip the box on the next commit.

T25–T28 are the operator-side discharge of the verifications this
very change exists to enable; they cannot legally run in the
implementer's sandbox (no sudo, no Chrome). `impl.md` lists the
exact commands the operator runs to discharge them; that is the
documented escape hatch the protocol allows.

## CHECKPOINTS

- C1 pnpm install — assumed (we ran every downstream command).
- C2 typecheck — ✅ green on `--filter=!playground`. The
  `apps/playground` failure is the pre-existing CI-regen artefact.
- C3 pnpm test — ✅ 9 tests pass. **Caveat (non-blocking):**
  `scripts/perf/run.test.mjs` is not picked up by `pnpm test`
  because root `package.json` has no test runner and the test uses
  `node:test`. Verification path is `node --test
scripts/perf/run.test.mjs`, which passes (11 assertions). The
  deviation is documented in `notes.md` "Deviation from design".
  See "Issues to address in a follow-up" below.
- C4 pnpm format:check — ✅ exit 0 (after restoring the
  implementer-committed `audit.md` / `perf.md`; re-running the
  audit/perf scripts during T2/T3 regenerates the un-prettier-
  formatted version that the committer's autoformat pass had
  already cleaned up at `3db80e5`).
- C5 audit:invariants — ✅ all PASS.
- C6 openspec validate — N/A (the doctor's `openspec-cli` check
  reports `openspec CLI not installed; running convention-only`,
  which is the documented pre-install state of the harness).
- C7 tasks.md — T25–T28 `[ ]` with justification in `impl.md`;
  T29 `[ ]` with effective evidence in `notes.md`. All other tasks
  `[x]`.
- C8 changeset — not required: this change touches `scripts/` and
  `autopilot/` only, not the CLI or any template's shippable
  output.
- C9 commits via `scripts/committer` — the wrapper prints a
  `committer: committed …` line to stdout but does not add a
  trailer to the commit message, so there is no in-message proof.
  The diff scope is the boundary check (see below) which matches
  `design.md`'s "Files touched" list exactly, which is the
  protocol's alternative verification path.
- C10 pnpm doctor — exit 0; 1 error (`feature-list` coherence for
  the **unrelated** `make-the-h1-contain-only-text` feature),
  3 warn (chrome absent — expected pre-install; npm-cache
  EROFS — expected pre-systemd-deploy; openspec-cli absent —
  pre-existing). The single error is outside this change's scope.
- C11 (templates-\* scoped) perf:budget passes — ✅ via graceful-skip.
- C12 scaffold:test — not run in this review (CI gate; not
  required by the protocol for non-CLI / non-template changes).

## Commits scoped to design.md

`git diff --name-only main` returns:

```
AGENTS.md
autopilot/AGENTS.md
autopilot/CLAUDE.md
autopilot/systemd/aig-runner.service
openspec/changes/wire-local-lighthouse-against-a-preview/APPROVED
openspec/changes/wire-local-lighthouse-against-a-preview/design.md
openspec/changes/wire-local-lighthouse-against-a-preview/proposal.md
openspec/changes/wire-local-lighthouse-against-a-preview/runs/2026-05-19T03-52-23Z/audit.md
openspec/changes/wire-local-lighthouse-against-a-preview/runs/2026-05-19T03-52-23Z/impl.md
openspec/changes/wire-local-lighthouse-against-a-preview/runs/2026-05-19T03-52-23Z/notes.md
openspec/changes/wire-local-lighthouse-against-a-preview/runs/2026-05-19T03-52-23Z/perf.md
openspec/changes/wire-local-lighthouse-against-a-preview/runs/2026-05-19T03-52-23Z/perf.txt
openspec/changes/wire-local-lighthouse-against-a-preview/specs/templates-perf/spec.md
openspec/changes/wire-local-lighthouse-against-a-preview/tasks.md
openspec/feature_list.json
openspec/progress/current.md
scripts/doctor/chrome-installed.mjs
scripts/doctor/install-chrome.mjs
scripts/doctor/npm-cache-writable.mjs
scripts/perf/run.mjs
scripts/perf/run.test.mjs
```

Every path is on `design.md`'s "Files touched" list (the
leader-authored `openspec/feature_list.json` and
`openspec/progress/current.md` are out-of-band per the harness
protocol and not under any prohibited prefix like `packages/*/src/`,
`apps/*/src/`, `openspec/specs/`, or `tests/`).

Branch commits on `spec/45-wire-local-lighthouse-against-a-preview`:

1. `f9bde05` — feature add to backlog (leader)
2. `568f2fc` — spec draft (spec_author)
3. `3aa2970` — approve / human gate (leader)
4. `54ed93a` — feat(autopilot): systemd unit + boundary doc
5. `d42af34` — feat(doctor): chrome installer + npm-cache-writable
6. `ac7d398` — feat(perf): wire Lighthouse runner + tests
7. `e8e7803` — docs(AGENTS): dual-gate perf + chrome install path
8. `e30101f` — chore(spec): impl.md + verification artefacts
9. `3db80e5` — chore: prettier autoformat

All within scope.

## Issues to address in a follow-up (not blocking this review)

1. **Wire `scripts/perf/run.test.mjs` into `pnpm test`.** Today
   the canonical test suite does not exercise the comparator; a
   future regression to `compareAgainstBudget` or `resolveTarget`
   could land without a red `pnpm test`. Two reasonable
   discharges: (a) add a root `package.json` `test` aggregator
   that invokes `node --test scripts/perf/*.test.mjs` after the
   workspace recursion, OR (b) declare a tiny `scripts/`
   workspace with vitest. Both expand `Files touched` past the
   current change's design and so belong in a follow-up. Open a
   feature backlog item.
2. **Flip T29 to `[x]` in `tasks.md`.** `notes.md` records the
   evidence; tasks.md just wasn't updated. One-line correction
   in a chore commit on this branch is fine, or fold into the
   next change.
3. **Operator-driven T25–T28.** Discharge during the post-merge
   runner provisioning. The expected outcome is a real
   per-metric Lighthouse run captured under
   `runs/<ts>/perf.txt` and the cleanup contract
   (`pgrep -af 'pnpm.*preview'` empty) confirmed in `notes.md`.

## Conclusion

The runner-perf pipeline is wired, the pure logic is unit-tested,
the audit and perf-budget gates are both green on this host via
the documented graceful-skip path, the boundary is tight, and
every operator-only verification has a documented discharge path.
Approving.

APPROVED -> openspec/changes/wire-local-lighthouse-against-a-preview/runs/2026-05-19T03-52-23Z/review.md
