---
title: Roadmap
description: How astro-ignite evolves toward the 0.2.0 launch and beyond.
lastUpdated: 2026-09-17
tags: [philosophy]
---

import Callout from '@/components/docs/Callout.astro';

The full roadmap lives in [`ROADMAP.md`](https://github.com/JordiParraCrespo/astro-ignite/blob/main/ROADMAP.md) at the repo root. The short version:

## Launch (0.2.0)

The first public release — npm still carries `0.1.0`, and the 0.2.0 changelog is accumulating as pending changesets under `.changeset/`. Two production-grade templates — `starter` and `docs` — that are well designed, AI-ready, documented end-to-end, and shipped through a stable release pipeline.

- **Two templates**, both Claude-designed, with i18n, SEO, JSON-LD, legal, theme, and a Lighthouse ≥95 mobile gate enforced in CI (100s are the aim, not the floor).
- **AI-ready**: every scaffold ships an `AGENTS.md` (with a `CLAUDE.md` symlink) so agents follow the locked practices when they edit your site.
- **Documented**: customization guides plus a live reference for the base components — 19 of the 24 atoms so far; the form atoms (`select`, `checkbox`, `switch`, `radio-group`) and `copy-button` still need pages.
- **shadcn-consumable**: the registry conforms to the shadcn schema, so any atom installs into any project via `npx shadcn@latest add @astro-ignite/<name>`.
- **AI-native docs**: per-page Markdown (`<path>.md`), `/llms.txt` and `/llms-full.txt`, and a "use this page with AI" menu — the static slice of the agent-first docs story.

## The version ladder

- **0.3.0** — more templates (a `blog` split); a native `astro-ignite add <component>` command + a registry MCP server; a registry _blocks_ tier (the docs site already previews a first block; PricingCard is next); the agentic docs layer (an MCP server generated from the docs, in-page Ask-AI search).
- **0.4.0** — deploy-target selection at scaffold time (Cloudflare Pages / Netlify / Vercel / Node) and deeper customization prompts.
- **Later** — View Transitions, more blocks, an examples gallery.

<Callout variant="note">
  This page is a summary. The canonical roadmap with rationale and risks lives
  in the GitHub repo, and it wins when the two disagree.
</Callout>
