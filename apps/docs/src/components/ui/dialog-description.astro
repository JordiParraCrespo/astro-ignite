---
import { cn } from '@/lib/cn';

export type Props = { class?: string };
const { class: className } = Astro.props;
---

<p class={cn('text-fg-muted text-sm leading-normal m-0', className)}><slot /></p>
